version: "3.8"
services:
  api:
    image: 'carton:latest'
    build:
      context: "./doojon"
    depends_on:
      - "db"
  db:
    image: "postgres:13"
  redis:
    image: "redis:6"
  proxy:
    image: "nginx:1.19.6"
    depends_on:
      - "api"
